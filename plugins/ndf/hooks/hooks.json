{
  "description": "Send Slack notification when Claude Code exits",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/scripts/slack-notify.sh session_end",
            "description": "Send Slack notification when Claude Code exits"
          }
        ]
      }
    ]
  }
}
