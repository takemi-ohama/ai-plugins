{
  "description": "Send Slack notification when Claude Code exits",
  "hooks": {
    "Stop": [
      {
        "type": "command",
        "command": "bash",
        "args": ["${pluginDir}/scripts/slack-notify.sh", "session_end"],
        "description": "Send Slack notification when Claude Code exits"
      }
    ]
  }
}
